public class SBR {
    // Для удобства редактирования продолжительность стадий представлена отдельными переменными
    // (в дальнейшем их целесообразно считывать из файла)
    private int stage1Duration = 8;
    private int stage2Duration = 2;
    private int stage3Duration = 3;
    private int stage4Duration = 2;
    private int stage5Duration = 1;
    
    private int cycleDuration = stage1Duration + stage2Duration + stage3Duration + stage4Duration + stage5Duration;
    // желательно добавить проверку на четность для целочисленного деления цикла пополам

    public int getCycleDuration(){
        return cycleDuration;

    }


    private int stageNumber = 0;

    // Типичный геттер (метод, возвращающий какое-либо значение)
    public int getStageNumber(){
        return stageNumber;

    }

    // Типичный сеттер (метод, переопределяющий что-либо)
    // Метод, изменяющий внутреннюю переменную для будущего экземпляра данного класса
    public void setStage(int currentHour, boolean aorb){
        
        int argHour = currentHour;
        // Обработка четности/нечетности емкости
        if (aorb == false){
            if (argHour < cycleDuration/2){
                argHour = argHour + cycleDuration/2;
            }else{
                argHour = argHour - cycleDuration/2;
            }
        }

        // вычисления номера текущей стадии       
        if (currentHour < stage1Duration){
            stageNumber = 1;
        }else if (stage1Duration < currentHour && currentHour < stage1Duration + stage2Duration){
            stageNumber = 2;
        }else if (stage1Duration + stage2Duration < currentHour && currentHour < stage1Duration + stage2Duration + stage3Duration){
            stageNumber = 3;
        }else if (stage1Duration + stage2Duration + stage3Duration < currentHour && currentHour < stage1Duration + stage2Duration + stage3Duration + stage4Duration){
            stageNumber = 4;
        }else{
            stageNumber = 5;
        }
    }

    //public void getCompressorState(int stageNumber){

    //public void getRecyclePumpState(int stageNumber){


}